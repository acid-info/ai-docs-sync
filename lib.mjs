// Pure functions only: config, globs, packing, gates, parsers. No process.env, no network, no
// side effects at import time, so test/ can import this without running the tool.

export const VERSION = '0.0.0';
